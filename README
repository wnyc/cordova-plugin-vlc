# notes compiling mobilevlckit for ios
# reference: https://wiki.videolan.org/VLCKit/#Building_the_framework_for_iOS

# clone the VLCKit repo
git clone git://git.videolan.org/vlc-bindings/VLCKit.git

cd VLCKit

# build the library for devices
./buildMobileVLCKit.sh

# wait a long time...

# build the library for the simulator
./buildMobileVLCKit.sh -s

# wait again...

# build the framework...
./buildMobileVLCKit.sh -f
 
# the resulting framework to be used in your project:
./build/MobileVLCKit.framework/  
