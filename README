To build the VLC framework for iOS, execute ./build_vlc_ios.sh. This will build the framework (including any custom patches in patches/ios) and copy it to src/ios and subsequently installed with the iOS plugin. Don't forget to commit and push the newly built framework after the script completes. 
